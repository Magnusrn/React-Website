import Terminal from "../components/terminal/Terminal";

function Timer() {
    return (
        <div>
            <h1>Terminal</h1>
            <Terminal/>
        </div>
    );
}

export default Timer;